package com.example.theisland.service;

import com.example.theisland.domain.Island;
import com.example.theisland.domain.IslandCell;

public class Simulation {
    public void runSim(int size, int iterations, int startPrey, int startPredator, int breedRate) {
/*        Island island = new Island();
        IslandCell[][] islandGrid = island.createIsland(size);
        island.populateIsland(islandGrid, startPrey, startPredator);
        for (int i = 0; i < iterations; i++) {
            island.moveAnimals(islandGrid);
            island.breedAnimals(islandGrid, breedRate);
            island.killAnimals(islandGrid);
        }*/
    }
}
